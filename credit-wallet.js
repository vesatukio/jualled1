/* DUTA LED - Ambil Barang legacy wallet adapter
   Ambil Barang is now ADMIN ONLY and is implemented by admin-ambil-barang.js.
   This file intentionally does not use localStorage or render a public wallet copy. */
(()=>{'use strict';
if(location.pathname.endsWith('/admin.html')||location.pathname.endsWith('admin.html'))return;
})();