/* DUTA LED - Admin Promo Header v2 */
(function(){
'use strict';
const KEY='duta_led_promo_header_v1';
const $=s=>document.querySelector(s), clean=s=>String(s??'').trim();
function load(){try{return JSON.parse(localStorage.getItem(KEY)||'[]').filter(x=>x&&x.image)}catch(e){return[]}}
function save(a){localStorage.setItem(KEY,JSON.stringify(a));window.dispatchEvent(new CustomEvent('dutaPromoUpdated',{detail:a}));render()}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function render(){const box=$('#promoHeaderList');if(!box)return;const a=load();box.innerHTML=a.length?a.map((p,i)=>`<div class="promo-admin-item" data-i="${i}"><img src="${esc(p.image)}" alt=""><div class="promo-admin-main"><b>${esc(p.title||'Promo '+(i+1))}</b><small>${esc(p.link||'Tanpa link')}</small></div><button type="button" data-action="up">↑</button><button type="button" data-action="down">↓</button><button type="button" data-action="toggle">${p.active===false?'OFF':'ON'}</button><button type="button" data-action="delete">×</button></div>`).join(''):'<div class="promo-empty">Belum ada promo header.</div>'}
function add(image,title,link){if(!image){alert('Masukkan URL gambar atau pilih file gambar.');return}const a=load();a.push({id:Date.now().toString(36),image,title:clean(title),link:clean(link),active:true});save(a)}
function bind(){const form=$('#promoHeaderForm');if(!form)return;const url=$('#promoImageUrl'),file=$('#promoImageFile'),title=$('#promoTitle'),link=$('#promoLink'),preview=$('#promoImagePreview');file?.addEventListener('change',()=>{const f=file.files?.[0];if(!f)return;if(!f.type.startsWith('image/')){alert('File harus gambar.');file.value='';return}if(f.size>2*1024*1024){alert('Ukuran maksimal 2 MB.');file.value='';return}const r=new FileReader();r.onload=()=>{url.value='';url.dataset.fileData=r.result;preview.src=r.result;preview.hidden=false};r.readAsDataURL(f)});url?.addEventListener('input',()=>{url.dataset.fileData='';const v=clean(url.value);preview.hidden=!v;if(v)preview.src=v});form.addEventListener('submit',e=>{e.preventDefault();add(clean(url.value)||url.dataset.fileData||'',title.value,link.value);form.reset();url.dataset.fileData='';preview.hidden=true});$('#promoHeaderList')?.addEventListener('click',e=>{const b=e.target.closest('button[data-action]');if(!b)return;const row=b.closest('.promo-admin-item'),i=Number(row?.dataset.i),a=load();if(!a[i])return;switch(b.dataset.action){case'delete':if(confirm('Hapus promo ini?'))a.splice(i,1);else return;break;case'up':if(i>0)[a[i-1],a[i]]=[a[i],a[i-1]];break;case'down':if(i<a.length-1)[a[i+1],a[i]]=[a[i],a[i+1]];break;case'toggle':a[i].active=a[i].active===false;break}save(a)});render()}
window.getPromoHeader=()=>load().filter(x=>x.active!==false);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();
